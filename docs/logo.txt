<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>KoreDataX Logo</title>

    <style>
        /* Reset */
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
            background: #e9e6e2;
            font-family: 'Georgia', serif;
        }

        .container {
            text-align: center;
        }

        /* SVG Scaling */
        svg {
            width: 300px;
            max-width: 90%;
        }

        /* Text Styling */
        .brand-text {
            margin-top: 20px;
            font-size: 48px;
            font-style: italic;
            color: #000;
            text-shadow: 2px 2px 2px rgba(0,0,0,0.2);
        }

        .brand-text span {
            color: #0b5c6b;
        }

        /* Responsive */
        @media (max-width: 600px) {
            .brand-text {
                font-size: 32px;
            }
        }
    </style>
</head>
<body>

<div class="container">

    <!-- SVG Logo -->
    <svg viewBox="0 0 300 200" xmlns="http://www.w3.org/2000/svg">

        <!-- Gradient -->
        <defs>
            <linearGradient id="grad" x1="0%" y1="0%" x2="100%" y2="0%">
                <stop offset="0%" stop-color="#00a8b5"/>
                <stop offset="100%" stop-color="#001f3f"/>
            </linearGradient>
        </defs>

        <!-- LEFT VERTICAL LINES (K shape) -->
        <line x1="40" y1="20" x2="40" y2="180" stroke="url(#grad)" stroke-width="10" stroke-linecap="round"/>
        <line x1="80" y1="20" x2="80" y2="180" stroke="url(#grad)" stroke-width="10" stroke-linecap="round"/>

        <!-- K DIAGONAL -->
        <path d="M80 100 L140 40" stroke="url(#grad)" stroke-width="10" fill="none" stroke-linecap="round"/>
        <path d="M80 100 L140 160" stroke="url(#grad)" stroke-width="10" fill="none" stroke-linecap="round"/>

        <!-- G / CIRCULAR SHAPE -->
        <path d="M150 50 
                 A70 70 0 1 1 150 150"
              stroke="#001f3f"
              stroke-width="10"
              fill="none"
              stroke-linecap="round"/>

        <!-- CIRCUIT LINES -->
        <line x1="150" y1="80" x2="250" y2="80" stroke="#001f3f" stroke-width="6"/>
        <line x1="150" y1="100" x2="250" y2="100" stroke="#001f3f" stroke-width="6"/>
        <line x1="150" y1="120" x2="250" y2="120" stroke="#001f3f" stroke-width="6"/>

        <!-- DOTS -->
        <circle cx="40" cy="20" r="5" fill="#00a8b5"/>
        <circle cx="80" cy="20" r="5" fill="#001f3f"/>
        <circle cx="250" cy="80" r="5" fill="#001f3f"/>
        <circle cx="250" cy="100" r="5" fill="#001f3f"/>
        <circle cx="250" cy="120" r="5" fill="#001f3f"/>

    </svg>

    <!-- Brand Name -->
    <div class="brand-text">
        KoreData<span>X</span>
    </div>

</div>

</body>
</html>