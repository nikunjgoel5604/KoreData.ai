import "@/styles/workspace.css";

export const metadata = {
  title: "Dashboard | KoreData-EX",
  description: "Enterprise AI Data Analytics Dashboard"
};

export default function DashboardLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
