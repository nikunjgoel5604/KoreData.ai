import "@/styles/workspace.css";

export const metadata = {
  title: "Workspace | KoreData-EX",
  description: "Enterprise AI Data Analytics Workspace"
};

export default function WorkspaceLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
